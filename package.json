{
  "name": "ool",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf ./dist",
    "pre": "pnpm clean && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "autoprefixer": "^10.4.21",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "modern-normalize": "^3.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">= 20.2.0",
    "pnpm": ">= 8.6.0"
  }
}
